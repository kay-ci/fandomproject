<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="FandomAppSpace.Views.InboxDisplayView">
  <StackPanel Margin="10,0,0,0">
    <StackPanel Width="200" Spacing="5">
          <TextBlock Text="Inbox:"/>
          <ItemsRepeater Items="{Binding Messages}">
            <ItemsRepeater.ItemTemplate>
              <DataTemplate>
              <StackPanel>
                <Border Margin="10,0,0,0" Padding="2">
                  <TextBlock Text="{Binding}"/>
                </Border>
                  <Button Content="View Message"
                    Command="{Binding $parent[Window].DataContext.View_Message}"
                    CommandParameter="{Binding}"/> 
                    </StackPanel> 
              </DataTemplate>
            </ItemsRepeater.ItemTemplate>
          </ItemsRepeater>
        </StackPanel>     
</StackPanel>
</UserControl>
